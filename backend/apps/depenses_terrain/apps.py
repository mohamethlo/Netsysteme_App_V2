from django.apps import AppConfig

class DepensesTerrainConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.depenses_terrain"
    verbose_name = "Dépenses Terrain"
